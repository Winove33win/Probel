{
  "name": "meu-site-react",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5173",
    "start": "node app.js",
    "prestart": "npm run build",
    "postinstall": "npm run build",
    "fix": "npm install @vitejs/plugin-react --save-dev && npm audit fix && rm -rf node_modules/.vite dist && (npx kill-port 3000 || true) && npm run build",
    "start:alt": "PORT=3001 node app.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "vite": "^5.2.0",
    "@vitejs/plugin-react": "^4.0.0"
  }
}
